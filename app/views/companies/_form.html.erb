<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, :value => (@name.nil? ? "": @name), :id=>"name" %>
  </div>

  <div class="field">
    <%= f.text_area :description, :id=>"content", :placeholder=>raw("[no description for this company has been added yet - add a description here]")%>
  </div>
  
  <div class="actions">
    <%= f.submit "add company", :class=>"btn btn-primary"%>
  </div>
<% end %>
